/**
 * 展示用的格式化工具。
 *
 * <h3>★ 为什么现在才把它提出来？</h3>
 *
 * <p>因为<b>到第四处了</b>。
 *
 * <p>{@code formatAmount} 这个「金额保留两位小数」的函数在本项目里
 * 前后出现过三次，每次都是就地内联的：
 * <pre>
 *   1. Cart.vue:164      内联在 selectedAmount 这个 computed 里
 *   2. Checkout.vue:422  内联在模板的格式化函数里
 *   3. Pay.vue:279       一个独立的 formatAmount 函数
 * </pre>
 *
 * <p>{@code Pay.vue} 当时就把判据写在自己的注释里了：
 * 「三份重复已经到临界点了 —— 如果出现第四处，就该把它提到
 * {@code utils/} 里去了。」「我的订单」页就是第四处，
 * 所以这一次把它提出来。
 *
 * <p><b>★ 为什么是「第四处」而不是「第二处」？</b>
 * 因为抽函数本身也有成本：多一个文件、多一层跳转、
 * 将来改的时候要多想一步「还有谁在用」。
 * 两处重复时，那个成本很可能比收益大；等到第四处，
 * 「改一处漏三处」的风险就明显更贵了。
 * <b>重复多少次才值得抽象，取决于每次重复的代价，而不是一个固定数字</b>——
 * 这里的代价是「金额显示不一致」，用户会以为算错了钱。
 *
 * <h3>⚠️ 这次【只改】Pay.vue 和 Orders.vue</h3>
 *
 * <p>{@code Cart.vue} 和 {@code Checkout.vue} 里那两份<b>暂时不动</b>，
 * 尽管它们看起来是同一个东西。原因有两个，都不是"懒得改"：
 *
 * <ol>
 *   <li><b>{@code Cart.vue:164} 那不是独立函数</b>，而是内联在
 *       {@code selectedAmount} 这个 computed 表达式里。
 *       要改它就得改动那个 computed 的写法 ——
 *       而它被购物车的「选中金额」逻辑依赖着，有测试盯着。
 *       这属于「顺手改掉别人依赖的东西」，风险方向和收益不成比例。</li>
 *   <li><b>{@code Checkout.vue:422} 同理</b>：它是结算页的一次性格式化。
 *       结算页是钱最容易出错的一页，为了一次纯粹的整理去动它，
 *       是拿最贵的页面去换最便宜的收益。</li>
 * </ol>
 *
 * <p>这正是 {@code Home.vue} 里已经立过的那条纪律：
 * <b>顺手改掉别人依赖的东西，是协作里最招人烦的一类改动。</b>
 * 要改就单独一轮、单独说明。
 */

/**
 * 金额显示：保留两位小数。
 *
 * <p><b>★ 这不是"好看一点"，是必需。</b>
 *
 * <p>后端 {@code BigDecimal} 的 {@code 228.70} 序列化成 JSON 数字之后
 * 变成 {@code 228.7} —— <b>尾随的 0 在 JSON 里不存在</b>
 * （{@code 228.70} 和 {@code 228.7} 是同一个数字，JSON 没有理由保留那个 0）。
 * 直接渲染就会看到「¥228.7」和「¥29.7」，而购物车页写的是「¥228.70」。
 * <b>同一笔钱在两个页面上长得不一样</b>，用户会以为哪里算错了。
 *
 * <p>⚠️ 所以这个函数的存在是<b>为了让「同一笔钱到处长得一样」</b>，
 * 而不是为了"格式漂亮"。理解了这一点，就知道它为什么必须只有一份 ——
 * 两份格式化函数就有两个答案，而它们服务的本来是同一个数字。
 *
 * <h3>★ 顺带说清边界：前端算的钱永远是显示，不是真相</h3>
 *
 * <p>真正的金额是后端用 {@code BigDecimal} 算好、存在
 * {@code DECIMAL(10,2)} 里的，这里做的是把那个数原样印出来。
 * 所以哪怕这里格式化错了，也不会让用户少付或多付一分钱。
 * <b>前端碰钱的地方，判断标准永远是「它会不会改变发出去的金额」——
 * 这个函数不会，所以它是安全的。</b>
 *
 * <p>（对比 {@code Checkout.vue} 里算合计的逻辑：那个也不会 ——
 * 提交给后端的只有商品 id 和地址，金额由后端重算。
 * 判断标准是同一句。）
 *
 * @param {number|string|null|undefined} v 后端给的金额（JSON 数字，可能是 null）
 * @returns {string} 形如 "228.70" 的字符串，<b>不带货币符号</b>
 *                   （符号由各页面自己加：¥ 还是 $ 是界面的事）
 */
export function formatAmount(v) {
  // ★ 用 Number(v || 0) 而不是 Number(v)：
  //   null / undefined / '' 都该显示成 0.00，而不是 NaN。
  //   后端配了 non_null，null 字段在 JSON 里直接消失 ——
  //   所以在 JS 这边读到的就是 undefined，这是一个【正常】的情况。
  return Number(v || 0).toFixed(2)
}
